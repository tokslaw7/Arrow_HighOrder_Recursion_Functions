const greet = (name) => {
    return "Hello, " + name + "!";
}
 

const square = (number) => {
    return number * number;
}

const isEven= (number) => {
    if (number % 2 === 0) {
        return true;
    } else {
        return false;
    }
}

 
 